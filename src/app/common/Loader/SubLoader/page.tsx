import { CircleLoader, MoonLoader, RiseLoader } from "react-spinners";

const LoaderMain = () => {
  return (
    <div className="p-3 m-3 flex justify-center items-center">
      <RiseLoader color="black" />
    </div>
  );
};

export default LoaderMain;
