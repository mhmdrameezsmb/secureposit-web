import { MoonLoader } from "react-spinners";

const Loader = () => {
  return (
    <div className="flex h-screen items-center justify-center bg-white">
      <MoonLoader color="#FFD34F" />
    </div>
  );
};

export default Loader;
